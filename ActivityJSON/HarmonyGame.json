{"name":"Harmony Game","func":"const hasAccidental = function(noteIndex){                let rem = noteIndex % 12;                return (rem === 1 || rem === 3 || rem === 6 || rem === 8 || rem === 10);                };                var noteVec = [\"C\", \"^C\", \"D\", \"^D\", \"E\", \"F\", \"^F\", \"G\", \"^G\", \"A\", \"^A\", \"B\"];        var answerVec = [\"I (unison)\", \"ii (Minor Second)\", \"II (Major Second)\", \"iii (Minor Third)\", \"III (Major Third)\"];                let noteIndices = [];        let noteStr = [];        document.getElementById(\"answerBtnDiv\").innerHTML = \"\";        document.getElementById(\"answerRes\").innerHTML = \"\";                noteIndices.push(Math.floor(Math.random() * 8));                do{            noteIndices.push((Math.floor(Math.random() * 4) + 1 + noteIndices[0]) % 12);        } while (noteIndices[0] === noteIndices[1]);                                for (let i = 0; i < noteIndices.length; i++){                        if (hasAccidental(noteIndices[i]) && Math.floor(Math.random() * 2)){                noteStr.push(\"_\" + noteVec[noteIndices[i] + 1]);                continue;            }                        noteStr.push(noteVec[noteIndices[i]]);            if (i > 0 && !hasAccidental(noteIndices[i]) && hasAccidental(noteIndices[i-1]) && noteStr[i - 1].includes(noteStr[i])){                noteStr[i] = \"=\" + noteStr[i];            }                    }                abcString = \"M:4/4%^%L:1/4%^%K:C%^%\" + noteStr[0] + \"2 \" + noteStr[1] + \"2|[\" + noteStr[0] + noteStr[1] + \"]4|\";                var correctIdx = noteIndices[1] - noteIndices[0];        var correctBtn = document.createElement(\"button\");        correctBtn.classList.add(\"answerBtn\");        correctBtn.innerHTML = answerVec[correctIdx];        var correctBtnIdNum = Math.floor(Math.random() * 3);        correctBtn.addEventListener(\"click\", function(){            document.getElementById(\"answerRes\").innerHTML = \"Correct!\";            document.getElementById(\"reloadBtnDiv\").style.visibility = \"visible\";        });                let prevWrongBtnIdx = -1;        for (let i = 0; i < 3; i++){                        if (i === correctBtnIdNum){                document.getElementById(\"answerBtnDiv\").appendChild(correctBtn);                continue;            }                        var wrongBtn = document.createElement(\"button\");            let btnTextIdx = Math.floor(Math.random() * answerVec.length);                        while (btnTextIdx === correctIdx || btnTextIdx === 0 || btnTextIdx === prevWrongBtnIdx){                btnTextIdx = Math.floor(Math.random() * answerVec.length);            }                        prevWrongBtnIdx = btnTextIdx;            wrongBtn.innerHTML = answerVec[btnTextIdx];            wrongBtn.classList.add(\"answerBtn\");                        wrongBtn.addEventListener(\"click\", function(){                document.getElementById(\"answerRes\").innerHTML = \"Sorry, please try again\";            });            document.getElementById(\"answerBtnDiv\").appendChild(wrongBtn);                    }            "}